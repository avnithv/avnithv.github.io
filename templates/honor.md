---
# Copy this file into src/content/honors/ and rename it.
# The filename is the slug. Add that slug to src/site-order.ts to set its order
# and whether it's featured on the home page (prefix with "*").
title: Award Name
org: Granting Organization        # optional
year: "2026"
description: "One-line summary — supports **bold**, _italics_, and [links](/path)."  # optional
# link: https://example.com      # optional. http(s):// -> external (↗, new tab); /path -> internal (→)
#                                 # NOTE: ignored in the list if this honor has a detail page (body
#                                 # below) — put the link in the body instead. Used only for no-page honors.
# linkText: view certificate     # optional label for that link (defaults to "learn more" / "more")
---

# OPTIONAL. Anything below the frontmatter creates a detail page at
# /honors/<slug> and makes the title link to it. Leave it empty for no page.
# Store materials (a certificate image, a PDF) in public/honors/<slug>/ and
# embed them here, e.g. ![certificate](/honors/<slug>/cert.png).
