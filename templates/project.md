---
# Copy this file into src/content/projects/ and rename it.
# The filename becomes the URL slug, e.g. my-thing.md -> /projects/my-thing
# Then add the slug to src/site-order.ts to set its order / feature it (prefix with "*").
title: Project Name
year: "2026"
summary: One-line description shown on the cards and at the top of the project page.
tags: ["tag-one", "tag-two"]   # shown as chips; also usable for grouping
# image: /projects/your-image.svg   # optional cover image (any format) placed in public/projects/
# link: https://example.com         # optional external link, shown as "visit project →" on the page
---

Write the full project write-up here in Markdown. This body renders on the
project's own page (/projects/<slug>).

## Overview

What it is and why it exists.

## Details

Results, methods, links, images, and code blocks are all supported.
